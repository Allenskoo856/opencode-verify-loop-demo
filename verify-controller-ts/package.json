{
  "name": "verify-controller-ts",
  "private": true,
  "version": "0.2.0",
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js",
    "verify": "node dist/verify-loop.js verify --profile auto"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3"
  }
}
